services:

  pgadmin:
    image: dpage/pgadmin4
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@admin.com
      PGADMIN_DEFAULT_PASSWORD: admin
    ports:
      - "5050:80"
    volumes:
      - pgadmin_data:/var/lib/pgadmin
    networks:
      - backend

  clipe_db:
    image: postgres:15
    environment:
      TZ: Europe/Moscow
      #POSTGRES_INITDB_ARGS: --timezone=Europe/Moscow
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: clipe
    command: postgres -c timezone=Europe/Moscow
    ports:
      - "8888:5432"
    volumes:
      - clipe_db_data:/var/lib/postgresql/data
    networks:
      - backend

  server:
    build: ./backend
    depends_on:
      - clipe_db
    env_file:
      - ./backend/.env
    ports:
      - "8080:8080"
    networks:
      - backend

  # frontend:
  #   build: ./frontend
  #   command: npm run dev -- --host 0.0.0.0 --port 5173
  #   depends_on:
  #     - server
  #   networks:
  #     - backend

volumes:
  clipe_db_data:
  pgadmin_data:

networks:
  backend:
    driver: bridge